# PR-09 · chore(repo): ordenar histórico, reparar codificaciones y documentar el estado real

**Rama:** `pr-09-higiene-y-estado` · **Parche:** `patches/PR-09-higiene-y-estado.patch` · **Depende de:** PR-08.

## Qué resuelve

`TAREAS.md` declaraba "PROYECTO COMPLETADO (VERSIÓN 1.0.0)". Había archivos scratch, una copia vieja del kit de UI, documentos con codificación rota y el README con el markdown roto.

## Cambios

- Histórico a `docs/historico/` con codificación reparada; `CAnti` recuperado (tenía 28 líneas únicas).
- Se borran borradores, duplicados y código sin uso (incluido `SalesReports.tsx`, que filtraba por un estado inexistente).
- `TAREAS.md` con backlog P0/P1/P2 y criterio de aceptación; `docs/ESTADO_QA.md` por requisito.

## Cómo se verificó

- `npm run verify` (guard, tipos, tests, build) y `npm run test:db:nodocker` (migraciones + pgTAP sobre PostgreSQL 16): en verde en este commit.
- Sin cambios de comportamiento; la suite completa sigue verde.

## Checklist QA

- [x] No edita migraciones existentes
- [x] Objetos nuevos en `public` revocan `anon`
- [x] Sin `expect(true)`, `.only` ni mocks nuevos en `src/`
- [x] Sin costos ni márgenes hacia Empleado (RNF-04)
- [x] No declara "completado" ni "listo para producción"
