# PR-06 · feat(clientes): cuentas y fiado reales con saldo por movimientos, tope y ajustes

**Rama:** `pr-06-clientes-y-fiado` · **Parche:** `patches/PR-06-clientes-y-fiado.patch` · **Depende de:** PR-05.

## Qué resuelve

Clientes hardcodeados y saldos solo en estado de React (CRÍTICO-04); sin página de clientes (CRÍTICO-06). RF-48 dice "ajustar o perdonar" pero un ajuste no tenía sentido (+/−).

## Cambios

- Página `/customers.html` con buscador (RF-45).
- Saldo = suma de movimientos (vista `customer_accounts`, RF-46); incluye lo registrado en el equipo y todavía no enviado.
- Columna `adjustment_sign`; solo la administradora ajusta (RF-48) y define el tope (RF-47).
- Fiado desde la caja con selector de cliente y aviso si supera el tope.
- `qa/mocks-conocidos.txt` queda vacío.

## Cómo se verificó

- `npm run verify` (guard, tipos, tests, build) y `npm run test:db:nodocker` (migraciones + pgTAP sobre PostgreSQL 16): en verde en este commit.
- `clientes_fiado_test.sql` (11): dos cajeros, un saldo; ajuste hacia arriba y hacia abajo. Mutación: ignorar el signo hace fallar el test.
- `balance.test.ts` (4).

## Checklist QA

- [x] No edita migraciones existentes
- [x] Objetos nuevos en `public` revocan `anon`
- [x] Sin `expect(true)`, `.only` ni mocks nuevos en `src/`
- [x] Sin costos ni márgenes hacia Empleado (RNF-04)
- [x] No declara "completado" ni "listo para producción"

## Para quien revise

Prueba manual: crear un cliente; fiarle desde la caja; registrar un abono sin conexión y ver el saldo actualizado al instante; con usuario administrador, ajustar y cambiar el tope; con empleado, verificar que no aparecen esas opciones.
