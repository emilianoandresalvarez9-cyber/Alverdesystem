# PR-08 · feat(admin): alta de sucursales y cajas

**Rama:** `pr-08-sucursales-y-cajas` · **Parche:** `patches/PR-08-sucursales-y-cajas.patch` · **Depende de:** PR-07.

## Qué resuelve

`BranchesManager` no se montaba y sus botones no hacían nada. Sin cajas cargadas, `/pos.html` no tiene qué elegir.

## Cambios

- Crear sucursales y cajas; archivar y reactivar cajas (RF-56). Pestaña nueva en Administración.

## Cómo se verificó

- `npm run verify` (guard, tipos, tests, build) y `npm run test:db:nodocker` (migraciones + pgTAP sobre PostgreSQL 16): en verde en este commit.
- `sucursales_test.sql` (5): contrato que usa la caja.

## Checklist QA

- [x] No edita migraciones existentes
- [x] Objetos nuevos en `public` revocan `anon`
- [x] Sin `expect(true)`, `.only` ni mocks nuevos en `src/`
- [x] Sin costos ni márgenes hacia Empleado (RNF-04)
- [x] No declara "completado" ni "listo para producción"
